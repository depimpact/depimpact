% \begin{algorithm}[!htb]

%     \KwIn{Graph(G)}
%     \KwOut{Graph(G)}
%     Map $\leftarrow$ This map could get the stack of edges based on the edge's source, target. \\
%     \For{$ \forall e \in G$}{
%         $stack \leftarrow Map.get(e)$ \; \\
%         \For{$\forall e' \in stack$}{
%             \If{$e$ and $e'$ have same system event type}{
%             $Merge(e, e')$}
%         }
%     }
%             \caption{Edge Merge}
%     \label{alg:merge}
% \end{algorithm}